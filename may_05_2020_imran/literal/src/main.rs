fn main() {
    let course = "PIAIC IOT"; // string literal , string slices
    let quarter = 1;
    println!("My course is {} and my quarter is : {}",course,quarter);
    let course = course.len(); //method length //usize
    println!("Length is : {}",course);
} 